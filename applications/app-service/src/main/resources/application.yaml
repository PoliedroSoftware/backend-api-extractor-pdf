##Spring Configuration
server:
  port: 8080
spring:
  application:
    name: backend-api-extractor-pdf
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: /h2
  profiles:
    include:

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when-authorized

